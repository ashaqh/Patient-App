# CareVault Patient Companion — Publish-Ready Plan

_Generated: 2026-05-23 | Flutter 3.38 | Riverpod | SQLite | 6-tab medical companion_

---

## 1. Current State Assessment

### What's Complete ✅
| Area | Status |
|---|---|
| Core features (medicines, prescriptions, follow-ups, timeline, vitals) | ✅ All 6 tabs functional |
| App lock / security (PIN, biometric, AES-256 encryption) | ✅ Fully implemented |
| Backup & restore (V3 passphrase crypto, staging, atomic DB swap) | ✅ All 10 bugs P0–P2 remediated |
| Clean Architecture (domain → data → presentation) | ✅ In place |
| Test scaffolding (unit, widget, integration stubs) | ✅ 3+ test files |
| Android debug APK builds successfully | ✅ |

### What's Missing / Needs Fixing Before Publish
| Area | Issue | Priority ||---|---|---|
| App icons | Android ok; iOS/Mac/Web placeholder references, no actual PNGs exist | P0 |
| Launch splash screen | Default Flutter launch theme — no branded splash | P0 |
| Release signing config | `build.gradle.kts` uses debug signing; `key.properties` missing | P0 |
| App store metadata | `name`, `description`, `keywords`, `privacyPolicyUrl` in `pubspec.yaml` incomplete | P0 |
| Privacy Policy & Terms screens | Privacy policy and terms of service screens are absent in-app | P0 |
| AndroidManifest | App label is `carevault` (lowercase); needs display-ready name | P0 |
| Deprecated APIs | 2 `DropdownButtonFormField` deprecation warnings remain | P1 |
| BuildContext async gaps | 42 warnings (non-blocking but should be cleaned before store submission) | P1 |
| Web/macOS icons | Referenced in Contents.json but files are absent | P1 |
| `publish_to` | Set to `'none'` in pubspec — must change for app store | P0 |
| HIPAA / health compliance notes | No compliance documentation page for reviewers | P2 |
| App store screenshots/marketing copy | Not created | P0 |
| Google Play listing assets (feature graphic, promo video optional) | Not created | P1 |
| `assets/images/` folder | Empty — needs blank-state illustrations, onboarding art | P1 |
| In-app "About" / "Rate this app" screen | Absent | P2 |

---

## 2. Phase Plan (Ordered for Fastest Path to Publish)

### PHASE 0 — Brand & Visual Identity
_(Est. 4–6 hours | Do before any code)_

**Goal:** Lock visual identity so all assets created below share the same look.

**Tasks:**
- [ ] Choose final app name: `CareVault` short name, `CareVault Patient Companion` long name
- [ ] Confirm color palette: primary (teal/medical blue), surface, neutral, accent
- [ ] Choose font pairing for the brand (already uses Inter + system via Google Fonts)
- [ ] Design simple, non-generic app icon concept (pill/cross/medical + shield, flat, high contrast)
- [ ] Decide avatar/logo style reference

---

### PHASE 1 — App Icon Generation & Platform Registration
_(Est. 4–8 hours | P0)_

**Goal:** Every platform has a branded, high-quality app icon.

#### 1a. Design source icon
- [ ] Create a 1024×1024 PNG source icon (signed/launcher icon) — single master file
- [ ] Guideline rules:
  - Must be recognizable at 48px (nav bar, notification)
  - No text in icon (replaced by label below icon)
  - Safe zone padding (≈10% on all sides)
  - Acceptable on both light and dark Android launcher backgrounds
- [ ] If not using an external designer, generate via SVG → raster (e.g., with `flutter_launcher_icons` workflow or manual resizing)

#### 1b. Android icons
- [ ] Run `flutter_launcher_icons` or use imagemagick to generate all mipmap densities (mdpi → xxxhdpi)
- [ ] Verify: `android/app/src/main/res/mipmap-*/ic_launcher.png` all replaced
- [ ] Verify foreground extraction from adaptive icon XML (API 26+)
- [ ] Generate Play Store 512×512 high-res listing icon

#### 1c. iOS icons
- [ ] Generate all sizes required by `ios/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json`:
  - 20×20 (2x/3x), 29×29 (2x/3x), 40×40 (2x/3x), 60×60 (2x/3x), 76×76, 83.5×83.5, **1024×1024**
- [ ] Place files in `ios/Runner/Assets.xcassets/AppIcon.appiconset/`
- [ ] Update `Contents.json` filenames to match sequence: keep as `Icon-App-*.png` or rename to `carevault_icon_*.png` consistently

#### 1d. macOS icons
- [ ] Generate all sizes from `macos/Runner/Assets.xcassets/AppIcon.appiconset/Contents.json`
- [ ] Files needed: `app_icon_16.png` through `app_icon_1024.png` (or equiv.)
- [ ] Place in `macos/Runner/Assets.xcassets/AppIcon.appiconset/`

#### 1e. Web icons
- [ ] Generate 192×192 and 512×512 PNG → place at `web/icons/Icon-192.png`, `web/icons/Icon-512.png`
- [ ] Generate maskable variants → `web/icons/Icon-maskable-192.png`, `web/icons/Icon-maskable-512.png`
- [ ] Update `web/manifest.json` `short_name` and `name` to `CareVault`

**Tools:**
- SVG source editor (Figma, Inkscape, or online)
- `flutter_launcher_icons` package (automates all densities)
- OR ImageMagick: `convert icon-1024.png -resize 512x512 output.png`

---

### PHASE 2 — Release Signing Configuration
_(Est. 2–3 hours | P0)_

**Goal:** Keystore ready so a release APK/AAB can be distributed.

**Tasks:**
- [ ] Generate keystore: `keytool -genkey -v -keystore carevault-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias carevault`
- [ ] Create `android/key.properties` (gitignored) with:
  ```properties
  storePassword=YOUR_STORE_PASSWORD
  keyPassword=YOUR_KEY_PASSWORD
  keyAlias=carevault
  storeFile=../carevault-release-key.jks   # or full path
  ```
- [ ] Update `android/app/build.gradle.kts`:
  ```kotlin
  signingConfigs {
      release {
          if (file("key.properties").exists()) {
              val properties = java.util.Properties()
              properties.load(java.io.FileInputStream(file("key.properties")))
              storeFile = file(properties["storeFile"] as String)
              storePassword = properties["storePassword"] as String
              keyAlias = properties["keyAlias"] as String
              keyPassword = properties["keyPassword"] as String
          }
      }
  }
  buildTypes {
      release {
          signingConfig = signingConfigs.getByName("release")
          isMinifyEnabled = true   // enable R8
          isShrinkResources = true
          proguardFiles("proguard-rules.pro")
          ndk { debugSymbolLevel = "symtabonly" }
      }
  }
  ```
- [ ] Create `android/app/proguard-rules.pro` (keep Flutter embeds, SQFlite, Riverpod)
- [ ] Test: `flutter build apk --release` + `flutter build appbundle --release`
- [ ] Remove `storePassword`/`keyPassword` from `key.properties` from accidental git-commits risk (double-check `.gitignore`)

---

### PHASE 3 — Branded Launch Splash Screen
_(Est. 2–3 hours | P0)_

**Goal:** Users see the CareVault brand immediately, not the default Flutter splash.

**Tasks:**
- [ ] Design a 384×512 (approximates 3:4 ratio) splash layout: centered icon + app name on brand-colored background
- [ ] Replace `android/app/src/main/res/drawable/launch_background.xml` (or create `launch_screen.xml`)
- [ ] Update Android native launch theme in `android/app/src/main/res/values/styles.xml`
- [ ] In Flutter, add a `SplashScreen` widget in `lib/main.dart` or a dedicated `splash_screen.dart`:
  - Show app name centered + a subtle brand animation (scale-in icon)
  - Transition to `App` after initializers complete (~2–3 seconds max)
  - Include a progress indicator so the "checking security" wait feels intentional
- [ ] For iOS: update `ios/Runner/Base.lproj/LaunchScreen.storyboard` with branded splash
- [ ] Smooth transition from splash → lock screen if app is locked

---

### PHASE 4 — In-App Legal & Compliance Screens
_(Est. 3–4 hours | P0)_

**Goal:** Privacy Policy and Terms screens satisfy app store requirements; content is HIPAA-aware.

**Tasks:**
- [ ] Create `lib/presentation/screens/privacy_policy_screen.dart`:
  - Plain language, short paragraphs
  - Covers: what data is collected, where stored (local-only), encryption, no cloud sync, app lock
  - "No data selling, no third-party sharing" explicit statement
  - GDPR / CCPA contact placeholder
- [ ] Create `lib/presentation/screens/terms_of_service_screen.dart`
- [ ] Add "Privacy Policy" and "Terms" links to Settings screen bottom area
- [ ] Consider: a one-time dismissible "Onboarding Consent" modal on first launch that includes "I agree to Privacy Policy & Terms of Service" checkbox
- [ ] (Optional) Populate policy content with a short markdown-formatted tooltip or FAQ

---

### PHASE 5 — AndroidManifest & App Display Names
_(Est. 1 hour | P0)_

**Goal:** App name is display-ready on device and notification bar.

**Tasks:**
- [ ] Change `android:label="carevault"` → `android:label="CareVault"` in `AndroidManifest.xml`
- [ ] Add `android:label` override for debug build variant if needed (separate from release)
- [ ] Verify `applicationId = "com.carevault.carevault"` is unique and final
- [ ] Add `android:roundIcon="@mipmap/ic_launcher_round"` if round icon placeholder is needed

---

### PHASE 6 — Pubspec Metadata & Build Config Cleanup
_(Est. 2 hours | P0/P1)_

**Goal:** App store listing metadata is populated and build is production-clean.

**Tasks:**
- [ ] Update `pubspec.yaml`:
  ```yaml
  name: carevault
  description: "CareVault is a simple, secure medication and appointment tracker built for elderly patients and caregivers. Manage prescriptions, medicine reminders, follow-ups, and health history — all offline, all private."
  version: 1.0.0+1
  publish_to: 'https://pub.dev'   # If publishing as library; otherwise keep 'none' for app-only
  ```
- [ ] Add keywords:
  ```yaml
  keywords:
    - medication
    - prescription
    - medicine
    - health
    - elderly
    - reminder
    - patient
    - caregiver
  ```
- [ ] Add `homepage`, `repository`, and `issue_tracker` fields if public on GitHub
- [ ] Update `android/app/src/main/AndroidManifest.xml` `android:label` values

---

### PHASE 7 — Fix Deprecated APIs & Build Warnings
_(Est. 4–6 hours | P1)_

**Goal:** Clean build before store submission (warnings = red flags in automated review).

**Tasks:**
- [ ] Replace `DropdownButtonFormField` with `DropdownButtonFormField` with `MenuAnchor`-based pattern (Flutter 3.22+ migration) in all screens (search for all usages first)
- [ ] Fix 42 BuildContext async gap warnings:
  - Replace `Navigator.of(context).push` after await with `if (!mounted) return;` guards
  - Use `mounted` check in all `FutureBuilder`, `async` callback overrides
  - Standard tool: `flutter analyze --watch` to iterate
- [ ] Remove 4 unused variable/element warnings after confirming not intentional API (e.g., `test_data_generator.dart` debug path)
- [ ] Add `const` where feasible across all screens for rebuild reduction
- [ ] Re-run `flutter analyze --no-fatal-infos` — target: 0 errors, 0 warnings

---

### PHASE 8 — Graphic Assets (Illustrations, Placeholders, Empty States)
_(Est. 3–6 hours | P1)_

**Goal:** `assets/images/` folder contains actual usable graphics; UI has no broken image placeholders.

**Tasks:**
- [ ] Empty-state illustrations (SVG/PNG) for:
  - No medicines added
  - No prescriptions uploaded
  - No follow-ups scheduled
  - No reminder logs
  - Empty timeline
- [ ] Onboarding / help illustrations (optional) for first-launch tour
- [ ] `assets/images/carevault_logo.png` (non-launcher, for in-app about/settings)
- [ ] Add assets to `pubspec.yaml` `flutter/assets` section
- [ ] Update empty-state widgets to use new images (`lib/presentation/widgets/common/empty_state.dart`)
- [ ] Prefer SVG where possible (via `flutter_svg`) for crisp rendering on all densities
- [ ] Audit all screens for hardcoded `Icons.help_outline`-style placeholders vs. proper illustrations

---

### PHASE 9 — Play Store Listing & Store Assets
_(Est. 4–8 hours | P0 — can be done in parallel with Phase 7/8)_

**Goal:** All Google Play Console assets prepared and ready for upload.

**Tasks:**
- [ ] App name: `CareVault – Medication & Appointment Tracker`
- [ ] Short description (80 chars — visible in search):
  ```
  Manage medicines, prescriptions & doctor visits. Private, offline, elderly-friendly.
  ```
- [ ] Full description (4000 chars max): structured with feature bullets, privacy focus, ELDERLY-friendly
- [ ] Screenshots (minimum 2, recommended 4–8 per form-factor):
  - Phone screenshots (1080×2400 or equivalent; showing dashboard, medicine list, add medicine flow, security settings)
  - Tablet screenshots if targeting tablets
- [ ] Feature graphic (1024×500): Brand + tagline
- [ ] App icon (512×512 from Phase 1)
- [ ] Category: `Medical` primary, `Health & Fitness` secondary
- [ ] Content rating: Fill `IARC` questionnaire
- [ ] Data safety form: honestly document local-only storage, no data sharing
- [ ] Privacy Policy URL: host the privacy policy from Phase 4 on a simple page/domain
- [ ] Target API level: confirm targetSdkVersion is 35 or latest (check Flutter SDK)
- [ ] Publish track decision: Internal → Closed alpha → Beta → Production

---

### PHASE 10 — iOS App Store Preparation (if targeting iOS)
_(Est. 4–8 hours | Initiated in parallel with Phase 9 if iOS is in scope)_

**Tasks:**
- [ ] Finalize bundle identifier: `com.carevault.patientcompanion` or `com.carevault.carevault`
- [ ] Confirm Apple Developer membership and Apple ID enabled
- [ ] Update iOS icons (Phase 1c complete → verify Contents.json references match actual files)
- [ ] Update iOS `Info.plist` with correct app name, privacy usage descriptions
- [ ] iOS launch storyboard: branded splash
- [ ] Expose app store metadata:
  - App Name, Subtitle (30 chars), Description (4000 chars)
  - Keywords (100 chars), Support URL, Marketing URL
- [ ] Choose build: iOS 14+ (minimum — check `ios/Podfile` / Xcode project)
- [ ] TestFlight distribution trial → App Store Connect submission

---

### PHASE 11 — Web / Desktop Release (optional)
_(Est. 2–4 hours | P2)_

**Tasks:**
- [ ] Run `dart run flutter_launcher_icons.main.dart` for web icons if using
- [ ] Fix `web/icons/` missing icon files (phase 1c)
- [ ] Update `web/manifest.json` — name, description, icons, `short_name`, theme_color
- [ ] Build web release: `flutter build web --release`; test locally
- [ ] Windows: run `flutter build windows --release`; confirm exe builds
- [ ] Decide if web/desktop builds are published at all (may hold for mobile-first)

---

### PHASE 12 — Final QA & Pre-Release Verification
_(Est. 4–6 hours | P0 — final gate before publishing)_

**Tasks:**
- [ ] `flutter clean && flutter pub get && flutter analyze` — 0 errors, 0 warnings
- [ ] `flutter test` — all existing tests pass
- [ ] `flutter build apk --release` — succeeds; APK installed on test device
- [ ] Smoke test on physical device covering all 6 tabs: no crashes, no blank screens
- [ ] Notifications: verify medicine + follow-up alarms fire on test device
- [ ] App lock: test PIN entry, biometric bypass, auto-lock timeout
- [ ] Backup: create a backup, delete data, restore — verify references intact
- [ ] Prescription upload: camera + gallery + file picker flow test
- [ ] Timeline export: CSV + JSON export files writable to Documents folder
- [ ] Security settings: all sub-settings accessible and change without crash
- [ ] GDPR / privacy: settings screen → Privacy Policy / Terms → open correctly
- [ ] Build info correct: app name = "CareVault", version = "1.0.0"
- [ ] Confirm no debug logs / `print()` calls leak in release (use `flutter build apk --release --obfuscate` spot-check)

---

### PHASE 13 — CI/CD & Versioning (nice-to-have pre-publish)
_(Est. 2–4 hours | P1 if time permits)_

**Goal:** Future builds are reproducible.

**Tasks:**
- [ ] Add GitHub Actions or simple local script for CI builds
- [ ] Add `.env.example` and `--dart-define` for feature flags (future: release channel tracking)
- [ ] Bump version to `1.0.1` or `1.1.0` for first post-refactor release; note changelog

---

## 3. Asset Inventory Checklist

### Icons (per platform)
| Platform | Sizes Required | Status | Action |
|---|---|---|---|
| Android mipmap-mdpi | 48×48 | ✅ Existing | Rename if branding changes |
| Android mipmap-hdpi | 72×72 | ✅ Existing | Rename |
| Android mipmap-xhdpi | 96×96 | ✅ Existing | Rename |
| Android mipmap-xxhdpi | 144×144 | ✅ Existing | Rename |
| Android mipmap-xxxhdpi | 192×192 | ✅ Existing | Rename |
| Android adaptive (foreground) | 432×432 | ❌ Missing | Create |
| iOS (all 10 sizes) | 20→1024px | ❌ Missing files/references | Generate & place |
| macOS (all 10 sizes) | 16→1024px | ❌ Missing files | Generate & place |
| Web 192×192 | 192×192 | ❌ Missing file | Generate & place |
| Web 512×512 | 512×512 | ❌ Missing file | Generate & place |
| Web maskable 192 | 192×192 | ❌ Missing | Generate & place |
| Web maskable 512 | 512×512 | ❌ Missing | Generate & place |
| Play Store 512 | 512×512 | ❌ Missing | Generate from master source |

### Splash / Launch Screens
| Platform | Status | Action |
|---|---|---|
| Android launch background XML | ⚠️ Default | Replace with branded `launch_screen.xml` |
| iOS LaunchScreen.storyboard | ⚠️ Default | Update with brand name + centered icon |
| Flutter splash widget | ❌ Missing | Add `SplashScreen` widget as entry point |

### UI Illustration Assets
| Asset | Status | Action |
|---|---|---|
| `assets/images/empty_medicines.svg` | ❌ Empty dir | Design + create |
| `assets/images/empty_prescriptions.svg` | ❌ Empty dir | Design + create |
| `assets/images/empty_followups.svg` | ❌ Empty dir | Design + create |
| `assets/images/empty_timeline.svg` | ❌ Empty dir | Design + create |
| `assets/images/carevault_logo.png` | ❌ Missing | Create for in-app branding |

---

## 4. Recommended Execution Order

```
Day 1  – Phase 0: Brand identity decisions (name, colors, font, icon concept)
         Phase 1a: Create master 1024×1024 source icon

Day 2  – Phase 1b–1e: Generate all platform icons
         Phase 2: Android release signing config
         Phase 3: Branded splash screens

Day 3  – Phase 4: Privacy Policy + Terms screens
         Phase 5: AndroidManifest display names
         Phase 6: pubspec.yaml metadata cleanup

Day 4  – Phase 7: Fix deprecations + BuildContext warnings
         Phase 8: UI illustration assets + empty state graphics

Day 5  – Phase 9: Google Play listing (descriptions, screenshots, graphics)
         Phase 12: Final QA + smoke test on device

Day 6  – Phase 10: iOS App Store (if in scope)
         Phase 11: Web / Desktop (if in scope)
         PUBLISH: Internal → Open Testing → Production
```

*(If iOS/web are not in scope, condensed to 3–4 days)*

---

## 5. Key Decisions Needed Before Starting

| # | Decision | Why Urgent | Default if un-decided |
|---|---|---|---|
| 1 | **App name final**: `CareVault` vs rename? | All icons, assets, legal copy flow from this | Use `CareVault Patient Companion` |
| 2 | **Primary platform**: Android only, or + iOS, or + Web? | Determines how many icon generations + store listings needed | Assume Android-first; iOS when ready |
| 3 | **Primary app color**: confirm teal/medical-blue is the brand color | App icon, splash, feature graphic all derive from it | Use existing `AppTheme.primaryColor` (`#0175C2`) |
| 4 | **Privacy policy hosting**: Self-hosted (GitHub Pages / Firebase Hosting) or third-party? | Needs a URL for Play Store submission | GitHub Pages on org repo |
| 5 | **Icon style**: Minimalist flat icon (one color + white) or detailed? | Determines design tool and time cost | Minimalist — fastest to produce |
| 6 | **Distribution track**: Internal → Closed Alpha → Beta → Prod? | Play Console track selection | Internal for 1 week, then Open Beta |

---

## 6. Risks & Mitigations

| Risk | Likelihood | Mitigation |
|---|---|---|
| Icon design doesn't match brand after decision | Medium | Lock brand decisions (Phase 0) before Phase 1 — do not rush icons |
| Release build still uses debug signing accidentally | Low | Verify `flutter build appbundle --release` APK signing info before upload |
| Store review rejects due to medical app claims | Medium | Keep app description general (no "diagnose" / "treat" claims); position as "tracker/companion" only |
| 42 BuildContext warnings don't resolve within plan timeline | Medium | Note: they are non-blocking; plan handles them as P1. themselves, automated test (do `dart fix && flutter format .`) to reduce count before Phase 7 |
| Privacy policy URL not reachable on first review attempt | Low | Host on a stable URL (GitHub Pages or Firebase Free) 72h before upload |

---

## 7. Success Criteria for "Publish-Ready"

All of the following must be true before hitting **"Submit to Review"** in the Play Console:

- [ ] App icon: branded, all platform sizes generated and correctly referenced
- [ ] Release keystore: configured; `--release` build produces a signed, minified APK/AAB
- [ ] `flutter analyze` passes with 0 errors, 0 warnings
- [ ] `flutter test` passes for all existing test suites
- [ ] All 6 tabs open without crash on a physical device (Android 10+)
- [ ] Privacy Policy and Terms are linked inside the app (even if T&C content is placeholder)
- [ ] `publish_to` field reflects real intent (`none` can be left for proprietary apps; no change needed unless publishing as a library)
- [ ] Play Store listing has: icon, feature graphic, screenshots, short + long description, content rating filled
- [ ] Privacy policy URL in Play Console matches Privacy Policy screen in app
- [ ] App is **not** using debug signing (verified with `apksigner verify --print-certs`)

---

_This plan was generated based on a full audit of `pubspec.yaml`, `build.gradle.kts`, `AndroidManifest.xml`, `memory-bank/` documentation, and the entire `lib/` source tree._
