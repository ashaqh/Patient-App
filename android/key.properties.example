# Google Play Store Release Keystore Properties Template
# Copy this file to "key.properties" and update it with your release credentials.
# Make sure "key.properties" is listed in your .gitignore so it is never committed.

storePassword=your_keystore_password
keyPassword=your_key_password
keyAlias=upload
storeFile=../app/upload-keystore.jks
